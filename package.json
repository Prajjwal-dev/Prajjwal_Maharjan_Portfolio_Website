{
  "name": "prajjwal-portfolio-react",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copy-assets": "node scripts/copy-assets.cjs",
    "generate-cert-data": "node scripts/generate-cert-data.cjs",
    "prepare-assets": "npm run copy-assets && npm run generate-cert-data"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
